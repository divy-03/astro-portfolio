---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/Icon.astro';

import {
  siTypescript,
  siExpress,
  siDocker,
  siLinux,
  siRedis,
  siReact,
  siNextdotjs,
  siPython,
  siFastapi,
  siPostgresql,
  siMysql,
  siPrisma,
  siMongodb,
  siRedux,
  siSass,
  siTailwindcss,
  siGraphql,
  siNginx,
  siWeb3dotjs,
  siPrometheus,
  siGrafana,
  siSocketdotio,
  siRabbitmq,
  siApachekafka,
  siJest,
  siN8n,
  siGithubactions,
  siModelcontextprotocol,
  siJsonwebtokens,
} from "simple-icons";

const siIcon = (icon: any) => icon.path;


const technicalSkills = [
  // Languages
  { name: "TypeScript", icon: siIcon(siTypescript), color: "#3178C6" },
  { name: "Python", icon: siIcon(siPython), color: "#3776AB" },

  // Frontend / Fullstack Frameworks
  { name: "React", icon: siIcon(siReact), color: "#61DAFB" },
  { name: "Next.js", icon: siIcon(siNextdotjs), color: "#FFFFFF" },
  // { name: "React Native", icon: siIcon(siReact), color: "#61DAFB" },

  // Backend Frameworks
  { name: "Express.js", icon: siIcon(siExpress), color: "#FFFFFF" },
  { name: "FastAPI", icon: siIcon(siFastapi), color: "#009688" },

  // Auth / API
  { name: "JWT", icon: siIcon(siJsonwebtokens), color: "#F59E0B" },
  { name: "GraphQL", icon: siIcon(siGraphql), color: "#E10098" },

  // Databases / ORM
  { name: "PostgreSQL", icon: siIcon(siPostgresql), color: "#4169E1" },
  { name: "MySQL", icon: siIcon(siMysql), color: "#4479A1" },
  { name: "MongoDB", icon: siIcon(siMongodb), color: "#47A248" },
  { name: "Prisma", icon: siIcon(siPrisma), color: "#2D3748" },
  { name: "Redis", icon: siIcon(siRedis), color: "#DC382D" },

  // State / Styling
  { name: "Redux", icon: siIcon(siRedux), color: "#764ABC" },
  { name: "Sass", icon: siIcon(siSass), color: "#CC6699" },
  { name: "Tailwind CSS", icon: siIcon(siTailwindcss), color: "#06B6D4" },

  // Infra / DevOps
  { name: "Docker", icon: siIcon(siDocker), color: "#2496ED" },
  { name: "Linux", icon: siIcon(siLinux), color: "#FCC624" },
  { name: "CI/CD", icon: siIcon(siGithubactions), color: "#2088FF" },
  { name: "Nginx", icon: siIcon(siNginx), color: "#009639" },

  // Realtime / Messaging
  { name: "Socket.io", icon: siIcon(siSocketdotio), color: "#FFFFFF" },

  // { name: "Web3", icon: siIcon(siWeb3dotjs), color: "#F16822" },
  // { name: "Prometheus", icon: siIcon(siPrometheus), color: "#E6522C" },
  // { name: "Grafana", icon: siIcon(siGrafana), color: "#F46800" },
  // { name: "RabbitMQ", icon: siIcon(siRabbitmq), color: "#FF6600" },
  // { name: "Kafka", icon: siIcon(siApachekafka), color: "#FFFFFF" },
  // { name: "Jest", icon: siIcon(siJest), color: "#C21325" },

  // Platforms / Tooling
  { name: "MCP Server", icon: siIcon(siModelcontextprotocol), color: "#38BDF8" },
  { name: "n8n", icon: siIcon(siN8n), color: "#EA4B71" },
];
---

<Layout title="Divy Pathak">
  <div class="stars"></div>
  <div class="nebula"></div>

  <main class="container">
    <!-- Hero Section -->
    <header class="hero">
      <div class="penguLogo" aria-hidden="true">
        <img class="penguImg" src="/penguLogo.png" alt="pengu">
      </div>

      <h1 class="title"><span class="title-main">Divy Pathak</span></h1>

      <p class="tagline" id="tagline">Crafting Backends that don't break.</p>

      <p class="description">
        A developer who lives in the terminal, runs on coffee, builds on the web, and dreams like a footballer.
      </p>
    </header>

    <!-- Terminal -->
    <section class="terminal">
      <div class="code-block">
        <div class="code-header">
          <span class="code-dot"></span>
          <span class="code-dot"></span>
          <span class="code-dot"></span>
        </div>
        <div id="code-oneliner" class="code-content">
          <div class="code-line comment" id="oneliner-comment"># Know me better on your terminal</div>
          <div class="code-line cmd">
            <span class="code-prompt">$</span>
            <span class="os-cmd">curl -fsSL https://nnnhub.duckdns.org/script.sh | bash</span>
            <button class="copy-line-btn" data-cmd="oneliner" title="Copy">
              <svg class="copy-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
              <svg class="check-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="display:none"><polyline points="20 6 9 17 4 12"/></svg>
            </button>
          </div>
        </div>
      </div>
      <p class="terminal-note">
        Works on Linux. On Windows use WSL/Git-bash terminal. The script logs about me on your terminal
      </p>
    </section>

    <script>
      const codeOneliner = document.getElementById('code-oneliner');

      const btn = document.querySelector(".copy-line-btn");
      btn.addEventListener('click', async () => {
        const code = "curl -fsSL https://nnnhub.duckdns.org/script.sh | bash";
        const copyIcon = btn.querySelector('.copy-icon');
        const checkIcon = btn.querySelector('.check-icon');

        try {
          await navigator.clipboard.writeText(code);
          btn.classList.add('copied');
          copyIcon.style.display = 'none';
          checkIcon.style.display = 'block';
          setTimeout(() => {
            btn.classList.remove('copied');
            copyIcon.style.display = 'block';
            checkIcon.style.display = 'none';
          }, 2000);
        } catch (err) {
          console.log("Failed to copy: ", err);
        }
      });
    </script>

    <!-- Tools and Skills -->
    <section class="technicalSkills">
      <h2 class="section-title">
        <span class="red-accent">⟩</span> Tools and Skills
      </h2>
      <div class="skills-row">
        {technicalSkills.map((p) => (
          <span class="skills-pill">
            <Icon icon={p.icon} color={p.color} size={16} />
            <span>{p.name}</span>
          </span>
        ))}
      </div>
    </section>

     <h2 class="section-title">
       <span class="red-accent">⟩</span> Contact me
     </h2>
    <!-- CTA Grid -->
    <nav class="cta-grid">

     <a href="mailto:pathakdivy03@gmail.com" class="cta cta-hashnode" target="_blank" rel="noopener">
        <svg class="cta-icon" role="img" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><title>Gmail</title><path d="M24 5.457v13.909c0 .904-.732 1.636-1.636 1.636h-3.819V11.73L12 16.64l-6.545-4.91v9.273H1.636A1.636 1.636 0 0 1 0 19.366V5.457c0-2.023 2.309-3.178 3.927-1.964L5.455 4.64 12 9.548l6.545-4.91 1.528-1.145C21.69 2.28 24 3.434 24 5.457z"/></svg>
        <span class="cta-label">Email</span>
        <span class="cta-sub">Mail me</span>
      </a>

      <a href="https://hashnode.com/@divy03" class="cta cta-email" target="_blank" rel="noopener">
        <svg class="cta-icon" role="img" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><title>Hashnode</title><path d="M22.351 8.019l-6.37-6.37a5.63 5.63 0 0 0-7.962 0l-6.37 6.37a5.63 5.63 0 0 0 0 7.962l6.37 6.37a5.63 5.63 0 0 0 7.962 0l6.37-6.37a5.63 5.63 0 0 0 0-7.962zM12 15.953a3.953 3.953 0 1 1 0-7.906 3.953 3.953 0 0 1 0 7.906z"/></svg>
        <span class="cta-label">Hashnode</span>
        <span class="cta-sub">Read my blogs</span>
      </a>

      <a href="https://github.com/divy-03" class="cta cta-github" target="_blank" rel="noopener">
        <svg class="cta-icon" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
        </svg>
        <span class="cta-label">GitHub</span>
        <span class="cta-sub">View my work</span>
      </a>

      <a href="https://www.linkedin.com/in/pathakdivy03" class="cta cta-linkedin" target="_blank" rel="noopener">
        <svg class="cta-icon" xmlns="http://www.w3.org/2000/svg" fill="currentColor"  viewBox="0 0 16 16">
          <path d="M0 1.146C0 .513.526 0 1.175 0h13.65C15.474 0 16 .513 16 1.146v13.708c0 .633-.526 1.146-1.175 1.146H1.175C.526 16 0 15.487 0 14.854zm4.943 12.248V6.169H2.542v7.225zm-1.2-8.212c.837 0 1.358-.554 1.358-1.248-.015-.709-.52-1.248-1.342-1.248S2.4 3.226 2.4 3.934c0 .694.521 1.248 1.327 1.248zm4.908 8.212V9.359c0-.216.016-.432.08-.586.173-.431.568-.878 1.232-.878.869 0 1.216.662 1.216 1.634v3.865h2.401V9.25c0-2.22-1.184-3.252-2.764-3.252-1.274 0-1.845.7-2.165 1.193v.025h-.016l.016-.025V6.169h-2.4c.03.678 0 7.225 0 7.225z"/>
        </svg>
        <span class="cta-label">LinkedIn</span>
        <span class="cta-sub">Let's connect</span>
      </a>
    </nav>

  </main>
</Layout>

<style>

  .red-accent {
    color: var(--coral-bright);
    font-weight: 700;
  }

  .section-title {
    font-family: var(--font-display);
    font-size: 1.4rem;
    font-weight: 600;
    margin-bottom: 0;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .stars,
  .stars::before,
  .stars::after {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    will-change: transform, opacity;
  }

  /* Base star layer */
  .stars {
    background-image:
      radial-gradient(2px 2px at 20px 30px, rgba(255,255,255,0.9), transparent),
      radial-gradient(1px 1px at 90px 40px, rgba(255,255,255,0.7), transparent),
      radial-gradient(2px 2px at 130px 80px, rgba(255,255,255,0.6), transparent),
      radial-gradient(1px 1px at 160px 120px, rgba(255,255,255,0.8), transparent),
      radial-gradient(2px 2px at 200px 60px, rgba(0,229,204,0.8), transparent),
      radial-gradient(1px 1px at 250px 150px, rgba(255,255,255,0.6), transparent),
      radial-gradient(2px 2px at 300px 40px, rgba(255,77,77,0.6), transparent);
    background-size: 400px 250px;

    animation:
      stars-move 180s linear infinite,
      stars-twinkle 6s ease-in-out infinite alternate;
  }

  /* Smaller stars */
  .stars::before {
    background-image:
      radial-gradient(1px 1px at 50px 80px, rgba(255,255,255,0.8), transparent),
      radial-gradient(1px 1px at 150px 200px, rgba(255,255,255,0.6), transparent),
      radial-gradient(1px 1px at 300px 100px, rgba(255,255,255,0.7), transparent),
      radial-gradient(1px 1px at 400px 50px, rgba(0,229,204,0.7), transparent);
    background-size: 500px 300px;

    animation:
      stars-move-slow 260s linear infinite,
      stars-twinkle-fast 4s ease-in-out infinite alternate;
    opacity: 0.8;
  }

  /* Bigger stars */
  .stars::after {
    background-image:
      radial-gradient(3px 3px at 100px 150px, rgba(255,255,255,0.9), transparent),
      radial-gradient(2px 2px at 350px 200px, rgba(255,77,77,0.8), transparent),
      radial-gradient(3px 3px at 500px 80px, rgba(0,229,204,0.8), transparent);
    background-size: 600px 400px;

    animation:
      stars-move-fast 120s linear infinite,
      stars-twinkle-slow 9s ease-in-out infinite alternate;
    opacity: 0.7;
  }

  /* 🚀 GPU-friendly movement (transform only) */
  @keyframes stars-move {
    from { transform: translate3d(0, 0, 0); }
    to   { transform: translate3d(-300px, 150px, 0); }
  }

  @keyframes stars-move-slow {
    from { transform: translate3d(0, 0, 0); }
    to   { transform: translate3d(-200px, 100px, 0); }
  }

  @keyframes stars-move-fast {
    from { transform: translate3d(0, 0, 0); }
    to   { transform: translate3d(-400px, 200px, 0); }
  }

  /* ✨ Twinkle (opacity only = cheap) */
  @keyframes stars-twinkle {
    from { opacity: 0.5; }
    to   { opacity: 0.9; }
  }

  @keyframes stars-twinkle-fast {
    from { opacity: 0.4; }
    to   { opacity: 1; }
  }

  @keyframes stars-twinkle-slow {
    from { opacity: 0.6; }
    to   { opacity: 0.85; }
  }

  /* Nebula Gradient */
  .nebula {
    position: fixed;
    inset: 0;
    background:
      radial-gradient(ellipse 80% 50% at 20% 20%, rgba(255, 77, 77, 0.12), transparent 50%),
      radial-gradient(ellipse 60% 60% at 80% 30%, rgba(0, 229, 204, 0.08), transparent 50%),
      radial-gradient(ellipse 90% 70% at 50% 90%, rgba(255, 77, 77, 0.06), transparent 50%);
    pointer-events: none;
    z-index: 0;
  }

  .container {
    position: relative;
    z-index: 1;
    max-width: 860px;
    margin: 0 auto;
    padding: 60px 24px 40px;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  /* Hero */

  .hero {
    text-align: center;
    margin-bottom: 56px;
    animation: fadeInUp 0.8s ease-out;
  }

  @keyframes fadeInUp {
    from { opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      ransform: translateY(0);
    }
  }

  .penguLogo {
    width: 200px;
    height: auto;
    margin: 0 auto;
    animation: float 4s ease-in-out infinite;
    cursor: pointer;
    transition: transform 0.3s ease;
  }

  .penguLogo:hover {
    transform: scale(1.1);
    animation: none;
  }

  .penguLogo img {
    width: 100%;
    height: 100%;
    filter: drop-shadow(0 0 20px rgba(255, 55, 255, 0.2));
    transition: filter 0.3s ease;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }

  .title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 10vw, 4.5rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1;
    margin-bottom: 12px;
  }

  .title-main {
    background: linear-gradient(135deg, var(--text-primary) 0%, var(--coral-bright) 50%, var(--cyan-bright) 100%);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 6s ease infinite;
  }

  @keyframes gradientShift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .tagline {
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 500;
    color: var(--coral-bright);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 20px;
    animation: fadeInUp 0.8s ease-out 0.15s both;
  }

  .tagline.dalek-mode {
    color: var(--cyan-bright);
    animation: shake 0.1s ease-in-out infinite;
    text-shadow: 0 0 10px var(--cyan-glow);
  }

  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-2px); }
    75% { transform: translateX(2px); }
  }

  .description {
    font-size: 1.1rem;
    color: var(--text-secondary);
    max-width: 780px;
    margin: 0 auto;
    line-height: 1.7;
    animation: fadeInUp 0.8s ease-out 0.3s both;
  }

  /* Terminal */
  .terminal {
    margin-bottom: 56px;
    animation: fadeInUp 0.8s ease-out 0.6s both;
  }

  .code-block {
    background: var(--bg-elevated);
    border: 1px solid var(--border-subtle);
    border-radius: 12px;
    overflow: hidden;
  }

  .code-header {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 16px;
    background: rgba(0, 0, 0, 0.3);
    border-bottom: 1px solid var(--border-subtle);
  }

  .code-content {
    padding: 16px 20px;
    font-family: 'SF Mono', 'Fira Code', 'Consolas', monospace;
    font-size: 0.9rem;
    line-height: 1.6;
  }

  .code-line {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 0;
  }

  .code-line.comment {
    color: var(--text-muted);
    font-style: italic;
  }

  .code-line.cmd {
    color: var(--text-primary);
  }

  .copy-line-btn {
    opacity: 0.6;
    margin-left: auto;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border: none;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.1);
    color: var(--text-muted);
    cursor: pointer;
    transition: all 0.2s ease;
    flex-shrink: 0;
  }

  .code-line:hover .copy-line-btn {
    opacity: 1;
  }

  .copy-line-btn:hover {
    background: rgba(255, 255, 255, 0.2);
    color: var(--text-primary);
  }

  .copy-line-btn.copied {
    opacity: 1;
    background: rgba(0, 229, 204, 0.2);
    color: var(--cyan-bright);
  }

  .copy-line-btn svg {
    width: 14px;
    height: 14px;
  }

  .code-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--text-muted);
  }

  .code-dot:nth-child(1) { background: #ff5f57; }
  .code-dot:nth-child(2) { background: #febc2e; }
  .code-dot:nth-child(3) { background: #28c840; }

  .terminal-note {
    margin-top: 16px;
    font-size: 0.9rem;
    color: var(--text-muted);
    text-align: center;
  }

  /* Tools and Skills */
  .technicalSkills {
    margin-bottom: 56px;
    animation: fadeInUp 0.8s ease-out 0.6s both;
  }

  .skills-row {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
    margin-bottom: 16px;
  }

  .skills-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 14px;
    border-radius: 20px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.6);
    backdrop-filter: blur(8px);
    font-size: 0.85rem;
    color: var(--text-secondary);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: default;

  }


  .skills-pill :global(svg) {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
    transition: all 0.2s ease;
  }

  .skills-pill:hover {
    border-color: var(--coral-bright);
    transform: translateY(-4px);
    box-shadow: 0 8px 24px color-mix(in srgb, var(--coral-bright) 20%, transparent);
  }

  /* CTA Grid */
  .cta-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-bottom: 56px;
    animation: fadeInUp 0.8s ease-out 0.45s both;
  }

  @media (min-width: 640px) {
    .cta-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    padding: 24px 16px;
    border-radius: 16px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.6);
    backdrop-filter: blur(12px);
    text-decoration: none;
    color: var(--text-primary);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .cta:hover {
    transform: translateY(-4px);
    border-color: var(--border-accent);
    box-shadow:
      0 12px 40px rgba(255, 77, 77, 0.15),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
  }

  .cta-icon {
    width: 28px;
    height: 28px;
    color: var(--coral-bright);
    transition: transform 0.25s ease;
  }

  .cta:hover .cta-icon {
    transform: scale(1.1);
  }

  .cta-email:hover { border-color: #5865F2; box-shadow: 0 12px 40px rgba(88, 101, 242, 0.2); }
  .cta-email:hover .cta-icon { color: #5865F2; }

  .cta-hashnode:hover { border-color: var(--cyan-bright); box-shadow: 0 12px 40px rgba(0, 229, 204, 0.15); }
  .cta-hashnode:hover .cta-icon { color: var(--cyan-bright); }

  .cta-github:hover { border-color: #f0f4ff; box-shadow: 0 12px 40px rgba(240, 244, 255, 0.1); }
  .cta-github:hover .cta-icon { color: #f0f4ff; }

  .cta-linkedin:hover { border-color: #0077B5; box-shadow: 0 12px 40px rgba(10, 102, 194, 0.15); }
  .cta-linkedin:hover .cta-icon { color: #0077B5; }

  .cta-label {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1rem;
  }

  .cta-sub {
    font-size: 0.8rem;
    color: var(--text-muted);
  }

</style>
